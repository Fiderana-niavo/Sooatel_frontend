const { Client } = require('pg'); const client = new Client({ user: 'postgres', password: 'postgres16', host: 'localhost', port: 5433, database: 'sooatel' }); client.connect().then(() => client.query('SELECT column_default FROM information_schema.columns WHERE table_name = ''employees'' AND column_name = ''employee_code''')).then(res => { console.log(JSON.stringify(res.rows)); process.exit(0); }).catch(err => { console.error(err); process.exit(1); });
